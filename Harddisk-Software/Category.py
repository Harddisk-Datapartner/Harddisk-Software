"""# ==========CATEGORY BUTTONS========== 
def show_category1():
    category1_frame = customtkinter.CTkFrame(master=install_frame, 
                                        fg_color=("#d1d0cf", "red"), 
                                        corner_radius=15)

    standardpakke_frame.pack_forget()
    category1_frame.pack(pady=5, padx=5, fill="both", expand=True)
    

def show_category2():
    category2_frame = customtkinter.CTkFrame(master=root, 
                                        fg_color=("#d1d0cf", "blue"), 
                                        corner_radius=15)

    standardpakke_frame.pack_forget()
    category2_frame.pack(pady=5, padx=5, fill="both", expand=True)

def show_category3():
    category3_frame = customtkinter.CTkFrame(master=root, 
                                        fg_color=("#d1d0cf", "green"), 
                                        corner_radius=15)

    standardpakke_frame.pack_forget()
    category3_frame.pack(pady=5, padx=5, fill="both", expand=True)



def show_category4():
    category4_frame = customtkinter.CTkFrame(master=root, 
                                        fg_color=("#d1d0cf", "yellow"), 
                                        corner_radius=15)

    standardpakke_frame.pack_forget()
    category4_frame.pack(pady=5, padx=5, fill="both", expand=True)

def show_category5():
    category5_frame = customtkinter.CTkFrame(master=root, 
                                        fg_color=("#d1d0cf", "cyan"), 
                                        corner_radius=15)

    standardpakke_frame.pack_forget()
    category5_frame.pack(pady=5, padx=5, fill="both", expand=True)

def show_category6():
    category6_frame = customtkinter.CTkFrame(master=root, 
                                        fg_color=("#d1d0cf", "gray"), 
                                        corner_radius=15)

    standardpakke_frame.pack_forget()
    category6_frame.pack(pady=5, padx=5, fill="both", expand=True)

def show_category7():
    category7_frame = customtkinter.CTkFrame(master=root, 
                                        fg_color=("#d1d0cf", "blue"), 
                                        corner_radius=15)

    standardpakke_frame.pack_forget()
    category7_frame.pack(pady=5, padx=5, fill="both", expand=True)

def show_category8():
    category8_frame = customtkinter.CTkFrame(master=root, 
                                        fg_color=("#d1d0cf", "black"), 
                                        corner_radius=15)

    standardpakke_frame.pack_forget()
    category8_frame.pack(pady=5, padx=5, fill="both", expand=True)

def show_category9():
    category9_frame = customtkinter.CTkFrame(master=root, 
                                        fg_color=("#d1d0cf", "white"), 
                                        corner_radius=15)

    standardpakke_frame.pack_forget()
    category9_frame.pack(pady=5, padx=5, fill="both", expand=True)

def show_category10():
    category10_frame = customtkinter.CTkFrame(master=root, 
                                        fg_color=("#d1d0cf", "red"), 
                                        corner_radius=15)

    standardpakke_frame.pack_forget()
    category10_frame.pack(pady=5, padx=5, fill="both", expand=True)

def show_category11():
    category11_frame = customtkinter.CTkFrame(master=root, 
                                        fg_color=("#d1d0cf", "red"), 
                                        corner_radius=15)

    standardpakke_frame.pack_forget()
    category11_frame.pack(pady=5, padx=5, fill="both", expand=True)

def show_category12():
    category12_frame = customtkinter.CTkFrame(master=root, 
                                        fg_color=("#d1d0cf", "red"), 
                                        corner_radius=15)

    standardpakke_frame.pack_forget()
    category12_frame.pack(pady=5, padx=5, fill="both", expand=True)

def show_category13():
    category13_frame = customtkinter.CTkFrame(master=root, 
                                        fg_color=("#d1d0cf", "red"), 
                                        corner_radius=15)

    standardpakke_frame.pack_forget()
    category13_frame.pack(pady=5, padx=5, fill="both", expand=True)
"""